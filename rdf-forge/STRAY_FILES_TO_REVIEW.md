# Stray Files To Review (Repo Root)

Automated hygiene sweep found these untracked artifacts at repo root. They look like botched heredoc / shell paste / fat-jar extraction accidents. User must decide on deletion.

All paths are relative to repo root: `C:\Users\giuli\repos\unified-rdf-platform\`.

| Path | Size | Content | Recommendation |
|---|---|---|---|
| `,` (single comma) | 491 B | Turtle/SHACL snippet (`@prefix ex:` ... `sh:NodeShape` ...). Clearly the tail of a paste where the newline before `,` was dropped. | **DELETE** — garbage from botched paste/heredoc. |
| `, ` (comma + space) | 4880 B | TypeScript/Angular fragment (`readonly createForm = this.fb.group(...)`). Truncated Angular component body. | **DELETE** — garbage from botched paste. |
| `.tsx` (just the extension) | 19 848 B | Full Angular component TS source (starts with `import { ChangeDetectionStrategy, Component, inject ... } from '@angular/core';`). Looks like the user ran `cat > .tsx` instead of a real filename. Check against `rdf-forge/rdf-forge-ui/src/app/**` for duplicate before deletion. | **INVESTIGATE** — likely dup of an existing component; confirm then delete. |
| `.被保险人/index.ts` | 609 B | Nonsense/corrupt text: `>export enum PipelineOb$eck$:` plus model-output-style `>### Response:>` lines. The directory name `被保险人` (Chinese, "the insured") is clearly not a real module. | **DELETE** — AI response output accidentally written to disk as a file. |
| `BOOT-INF/lib/rdf-forge-common-1.0.0-SNAPSHOT.jar` | (dir) | Output of a Spring Boot fat-jar `jar -xf` extraction at repo root. | **DELETE** — rebuild via `mvn package` if needed. |
| `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` | (dir) | Same fat-jar extraction at repo root. | **DELETE** — regenerated by build. |
| `io/rdfforge/common/config/NoAuthSecurityConfig.class` | (dir + class) | Same fat-jar extraction at repo root. A `.class` file should never live next to source. | **DELETE** — rebuild via `mvn compile` if needed. The real source is at `rdf-forge/rdf-forge-common/src/main/java/io/rdfforge/common/config/NoAuthSecurityConfig.java`. |
| `tmp-shacl.jar` | ~154 MiB | Large fat-jar left from a one-off SHACL test. Obvious throwaway. | **DELETE** — reclaim ~154 MiB of disk. |

## Suggested one-liner (USER RUNS, not automation)

```bash
# From repo root. VERIFY each file first.
rm -f "," ", " .tsx tmp-shacl.jar
rm -rf BOOT-INF META-INF io .被保险人
```

## Notes

- `.gitignore` has been updated to prevent accidental commits of similar files in the future (`BOOT-INF/`, `META-INF/`, `io/`, `tmp-*.jar`, `**/*.tmp`).
- None of these files are currently tracked in git; `git rm` is not required.
- The `.tsx` file is the only one that might contain real work — diff it against `rdf-forge/rdf-forge-ui/` before deleting.
